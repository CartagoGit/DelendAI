# AGENTS.md — working in `@delendai/core`

> **Source of truth:** [`docs/delendai/AGENT-BOOTSTRAP.md`](docs/delendai/AGENT-BOOTSTRAP.md).
> Read that file once per session — it is the **only** place agent rules
> live. Do not enumerate tool / skill / proposal ids in your answers; the
> server (`delendai_overview`, `delendai_agent_catalog`) is the live
> source of truth and changes every week.
>
> **Host appendix in effect:** §7 (repo-level rules).

This file is a pointer. All content lives in the bootstrap.

<!-- delendai:begin -->

# delendai host hints (auto-generated)

See `docs/delendai/host-hints/agent-instructions.generated.md` for the live agent catalog.

<!-- delendai:end -->
