/** Constants for `./sync-with-integration.script`. */

/** Used when no project config declares one. */
export const DEFAULT_INTEGRATION_BRANCH = 'develop';

/** The only remote this repo has, and the only one worth syncing from. */
export const SYNC_REMOTE = 'origin';
