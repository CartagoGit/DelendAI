/**
 * commit-policy WIP stub — f00181 deferred.
 *
 * The full plugin (commit authority wrapping git primitives) is being
 * built in /tmp/commit-policy-WIP-src-backup-* by a follow-up pass.
 * See f00181 frontmatter for the deferred-reason and the list of
 * 6 typecheck errors that exceeded the q00005 closure scope.
 */
export const COMMIT_POLICY_STUB = true;
