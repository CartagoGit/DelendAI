/**
 * `@mcp-vertex/test-kit` entrypoint. Re-exports the public surface so
 * `@mcp-vertex/test-kit` resolves to the same surface as
 * `@mcp-vertex/test-kit/public`.
 */
export * from './public/index';
