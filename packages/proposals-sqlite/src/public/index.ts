/**
 * public/index.ts — q00022 S1.
 *
 * Stable public surface. Hosts consume the migrations + driver from
 * here. Future slices (q00022 S2+) add the repository layer to this
 * barrel.
 */
export * from '../index';
